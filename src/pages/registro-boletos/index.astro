---
import Layout from '../../layouts/Layout.astro';
import RegistrationForm from '../../components/RegistrationForm';
---

<Layout title="Registro de Boletos | FECIIT 2026" navTheme="light">
    <main class="min-h-screen bg-slate-50 pt-32 pb-20 px-6 relative overflow-hidden">
        <!-- Decoration -->
        <div class="absolute top-0 right-0 w-[50vw] h-[50vw] bg-feciit-blue/5 rounded-full blur-[120px] -translate-y-1/2 translate-x-1/2 pointer-events-none"></div>
        <div class="absolute bottom-0 left-0 w-[40vw] h-[40vw] bg-purple-500/5 rounded-full blur-[100px] translate-y-1/2 -translate-x-1/3 pointer-events-none"></div>

        <div class="relative z-10 max-w-7xl mx-auto">
            <div class="grid lg:grid-cols-2 gap-16 items-center">
                <div class="space-y-8">
                    <span class="inline-block py-1 px-3 rounded-full bg-blue-50 text-feciit-blue font-bold text-xs uppercase tracking-[0.2em]">
                        Ticket Portal
                    </span>
                    <h1 class="text-5xl md:text-7xl font-bold text-feciit-dark tracking-tighter leading-none">
                        Tu puerta a la<br/>
                        <span class="text-transparent bg-clip-text bg-gradient-to-r from-feciit-blue to-purple-600">Innovación</span>
                    </h1>
                    <p class="text-xl text-gray-500 max-w-md leading-relaxed">
                        Regístrate ahora y asegura tu lugar en la feria de ciencias más importante de la región. Acceso gratuito a conferencias, exposiciones y networking.
                    </p>
                    
                    <div class="flex gap-4 pt-4 text-sm font-bold text-gray-400">
                        <div class="flex items-center gap-2">
                             <div class="w-2 h-2 rounded-full bg-green-500"></div>
                             QUEDAN LUGARES
                        </div>
                        <div class="flex items-center gap-2">
                             <div class="w-2 h-2 rounded-full bg-feciit-blue"></div>
                             ACCESO LIBRE
                        </div>
                    </div>
                </div>

                <div class="relative">
                    <RegistrationForm client:load />
                </div>
            </div>
        </div>
    </main>
</Layout>
